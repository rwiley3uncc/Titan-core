"""Temporary Titan-core compatibility namespace.

The canonical application identity is now Titan BattleBuddy via the
``titan_battlebuddy`` package. This package remains available during the staged
migration so existing imports and startup commands continue to work.
"""
